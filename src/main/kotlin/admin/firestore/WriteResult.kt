package admin.firestore

/**
 * A WriteResult wraps the write time set by the Firestore servers on sets(), updates(), and creates().
 * @see <a href="https://cloud.google.com/nodejs/docs/reference/firestore/0.10.x/WriteResult">WriteResult</a>
 */
external interface WriteResult